if (swiperRef.current) {
      const current = swiperRef.current.activeIndex;
      const len = images.length;
      const prevIndex = (current - 1 + len) % len;
      const nextIndex = (current + 1) % len;

      if (index === current) {
        // Clicked center image, do nothing
        return;
      } else if (index === prevIndex) {
        // Clicked left image, go to previous using slideToLoop for infinite loop
        swiperRef.current.slideToLoop(prevIndex);
      } else if (index === nextIndex) {
        // Clicked right image, go to next using slideToLoop for infinite loop
        swiperRef.current.slideToLoop(nextIndex);
      } else {
        // Clicked other image, slide to it
        swiperRef.current.slideToLoop(index);
      }
    }
  };
=======
  const handleClick = index => {
    if (swiperRef.current) {
      const current = swiperRef.current.activeIndex;
      const len = images.length;
      const prevIndex = (current - 1 + len) % len;
      const nextIndex = (current + 1) % len;

      console.log('handleClick:', { current, prevIndex, nextIndex, clickedIndex: index });

      if (index === current) {
        // Clicked center image, do nothing
        return;
      } else if (index === prevIndex) {
        // Clicked left image, go to previous using slideToLoop for infinite loop
        swiperRef.current.slideToLoop(prevIndex);
      } else if (index === nextIndex) {
        // Clicked right image, go to next using slideToLoop for infinite loop
        swiperRef.current.slideToLoop(nextIndex);
      } else {
        // Clicked other image, slide to it
        swiperRef.current.slideToLoop(index);
      }
    }
  };
